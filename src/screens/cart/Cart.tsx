import Layout from "../../components/layout/Layout";
import "./Cart.scss";
import { FC } from "react";

const Cart: FC = () => {
  return (
    <Layout sidebar={false}>
      <section>
        <div className="section-title">Contact</div>
        <p>TO DO: Contact/Feedback Form</p>
      </section>
    </Layout>
  );
};

export default Cart;
